services:
  toppay:
    image: toppay:latest
    build: .
    env_file:
      - ./configs/.env
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1'